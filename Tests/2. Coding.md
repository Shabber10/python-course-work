# 2. Coding - Coding and Short Answer Questions

This file contains all coding and short answer questions from the course, organized by topic.

---

## Section A: Daily Practice Tests

### Day 05-06: Strings (Basics & Operations)

#### Q1. Write a program to take tuple as input and print sum of all elements

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
import ast
a=list(ast.literal_eval(input()))
print(sum(a))
```

#### Q2. Write a Python function to format a number n as 'n units' using the dot format method.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
print(a,"units")
```

---

### Day 07: Lists

#### Q3. Write a program to print the length of string

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=input()
print(len(a))
```

#### Q4. A security system validates passcodes by comparing specific character positions and checking for keywords. Write a program that reads a string passcode, a positive index i, and a negative index j. The passcode is considered 'VALID' only if the character at positive index i is identical to the character at negative index j, and the substring 'secret' exists within the passcode.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=input()
b=int(input())
c=int(input())


if a[b] == a[c] and "secret" in a: 
   print("VALID")
else:
  print("INVALID")
```

#### Q5. Write a python program to print the last element of the given list

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=list(map(int,input().split(",")))
print(a[-1])
```

---

### Day 08: Tuples

#### Q6. Write a function to replace all vowels in a string with a given character. Vowels include: a, e, i, o, u (both lowercase and uppercase). Input Format : First line: A single string s (can contain spaces and punctuation). Second line: A single character c (replacement character). Output Format : Print the modified string with all vowels replaced by the given character.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
def replace_vowels(s,c):
  vowels="aeiouAEIOU"
  result=""
  for ch in s:
    if ch in vowels:
      result+=c
    else:
      result+=ch
  return result

s=input()
c=input().strip()

print(replace_vowels(s,c))
```

#### Q7. Write a function that removes all negative numbers from a given list and returns the updated list.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
def remove_negative(lst):
  return[x for x in lst if x>=0]

arr=list(map(int,input().strip().split(',')))

print(remove_negative(arr))
```

---

### Day 10: Dictionaries & Collections

#### Q8. Write a function to count how many times a given substring occurs in the main string. Input Format: First line: A string s (the main string). Second line: A string sub (the substring to search for). Output Format: Print a single integer: the number of times the substring appears in the main string (including overlapping matches).

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
s,sub=input().strip(),input().strip()

cnt=sum(1 for i in range(len(s)) if s[i:i+len(sub)]==sub)

print(cnt)
```

#### Q9. Write a function that adds a given element to a set.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=set(map(int,input().split(',')))
b=int(input())

a.add(b)
print(a)
```

---

### Day 12: Conditional Statements

#### Q10. You are given an integer value representing the temperature in degrees Celsius. Based on the temperature, classify and print the weather condition as follows: Print "Freezing" if the temperature is less than 0 Print "Cold" if the temperature is between 0 and 15 (inclusive) Print "Warm" if the temperature is between 16 and 30 (inclusive) Print "Hot" if the temperature is greater than 30 Input Format: A single integer T — the temperature in Celsius. Output Format : Print one of the following:"Freezing" ,"Cold" , "Warm" and "Hot"

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())

if 0<=a<=15:
  print("Cold")
elif 16<=a<=30:
  print("Warm")
elif a>30:
  print("Hot")
elif a<0:
  print("Freezing")
```

#### Q11. Takes an integer input representing a person's age. Classifies the age into one of the following groups

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())

if 0<=a<=12:
  print(f"Age {a} is Child")
elif 13<=a<=19:
  print(f"Age {a} is Teenager")
elif 20<=a<=59:
  print(f"Age {a} is Adult")
elif a>=60:
  print(f"Age {a} is Senior")
```

---

### Day 13: Loops (While and For Loops)

#### Q12. You are given the type of bank account, the withdrawal amount, and the current balance. Depending on the type of account, determine the result of a transaction using the following rules: Rules: If account_type is "savings": If amount > balance → Output: "Insufficient" Else: If amount > 25000 → Output: "OTP Required" Else → Output: "Success" If account_type is "current": Overdraft of ₹10,000 is allowed. If amount ≤ balance + 10000 → Output: "Success with overdraft" Else → Output: "Declined" Input Format: First line: a string account_type. , Second line: an integer amount. and Third line: an integer balance Output Format: A single line indicating the result of the transaction:"Success" , "OTP Required" , "Insufficient" , "Success with overdraft" and "Declined"

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
c=input()
a=int(input())
b=int(input())

if c=="savings":
    if a>b:
      print("Insufficient")
    else:
      if a>25000:
        print("OTP Required")
      else:
        print("Success")

if c=="current":
    if a<=b+10000:
        print("Success with overdraft")
    else:
        print("Declined")
```

#### Q13. You are given N different colors numbered from 1 to N. Chef has Aᵢ balls of color i (1 ≤ i ≤ N). Chef will arrange some boxes and must put each ball in exactly one box. Your task is to find the minimum number of boxes required such that: No box contains two balls of the same color. Input Format : The first line contains an integer T, the number of test cases. For each test case: The first line contains an integer N. The second line contains N space-separated integers A₁, A₂, …, Aₙ. Output Format : For each test case, print a single integer — the minimum number of boxes required so that no box contains balls of the same color.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
b=int(input())
c=list(map(int,input().split()))

print(max(c))
```

#### Q14. You are given n projects. Each project i has: a pure profit → profits[i] a minimum capital required → capital[i] Initially, you have w units of capital. When you finish a project, you earn its profit, and it is added to your total capital. Your task is to select at most k distinct projects such that your final capital is maximized. Return the maximum possible final capital. The answer fits in a 32-bit signed integer. Input Format : k w n profits[0] profits[1] ... profits[n-1] capital[0] capital[1] ... capital[n-1] Output Format : final_capital

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
import heapq
k,w=map(int,input().split())
n=int(input())
p=list(map(int,input().split()))
c=list(map(int,input().split()))

project=sorted(zip(p,c))

max_heap=[]
i=0

for _ in range(k):
  while i < n and project[i][0]<=w:
    heapq.heappush(max_heap,-project[i][1])
    i+=1

  if not max_heap:
    break

  w-=heapq.heap.heappop(max_heap)

print(w)
Correct Solution

k, w = map(int, input().split())
n = int(input())
profits = list(map(int, input().split()))
capital = list(map(int, input().split()))
projects = []
for i in range(n):
    projects.append([capital[i], profits[i]])
projects.sort()
i = 0
available = []
for _ in range(k):
    while i < n and projects[i][0] <= w:
        available.append(projects[i][1])
        i += 1
    if not available:
        break
    max_profit = max(available)
    w += max_profit
    available.remove(max_profit)
print(w)
```

#### Q15. There are n couples sitting in 2n seats arranged in a row. The people and seats are represented by an integer array row where row[i] is the ID of the person sitting in the i-th seat. Couples are numbered in order: the first couple is (0, 1), the second is (2, 3), and so on. Return the minimum number of swaps so that every couple is sitting side by side. A swap consists of choosing any two people and switching their seats. Input Format : The first line contains an integer n — the number of couples. The second line contains 2n space-separated integers row[i] — the IDs of people sitting in order. Output Format : Print a single integer — the minimum number of swaps required.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
n=int(input())
row=list(map(int,input().split()))

swaps=0

for i in range(0,2*n,2):
  partner=row[i]^1
  if row[i+1]!=partner:
    swaps+=1
    idx=row.index(partner)
    row[i+1],row[idx]=row[idx],row[i+1]

print(swaps)
```

#### Q16. Write a program that prints numbers from 1 up to a user-entered number N using a while loop.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
for i in range(1,a+1):
  print(i)
```

#### Q17. Write a Python program that reads two integers and finds their greatest common divisor (GCD) using the Euclidean algorithm implemented with a while loop. Input Format: A single line containing two integers separated by space. Output Format: A single integer representing the GCD of the two numbers.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a,b=list(map(int,input().split()))
while b!=0:
  a,b=b,a%b

print(a)
```

#### Q18. Write a program that takes an integer n as input and prints all numbers from 1 to n, each separated by a space, using a for loop. Input Format:A single integer n (upper range). Output Format: Print the numbers from 1 to n (inclusive) in a single line separated by spaces.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
for i in range(1,a+1):
  print(i,end=" ")
```

#### Q19. Write a program to concatenate two strings

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=input()
b=input()
print(a+" "+b)
```

---

### Day 16-17: Pattern Printing (Alphabet & Basic)

#### Q20. Write a program that reads N key-value pairs (where both keys and values are integers), stores them in a dictionary, and returns the most frequent value. If multiple values have the same highest frequency, return the smallest such value. Input Format:The first line contains an integer N — the number of key-value pairs.The next N lines each contain two space-separated integers: key and value. Output Format:Print the most frequent value in the dictionary.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
b={}
for _ in range(a):
  key,value=map(int,input().split())
  b[key]=value


seen={}
for v in b.values():
  if v in seen:
    seen[v]+=1
  else:
    seen[v]=1

m=max(seen.values())
print(min(k for k in seen if seen[k]==m))
```

#### Q21. Print a right-aligned triangle pattern of stars (*). Given an integer n, print a pattern of stars where the first line has 1 star right-aligned, the second line has 2 stars right-aligned, and so on until the nth line with n stars.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
for i in range(1,a+1):
  for k in range(1,a-i+1):
      print(" ",end="")
  for j in range(i):
    print("*",end="")
  print()
```

#### Q22. ou are given an M x N binary matrix containing only 0s and 1s. Your task is to find the size (side length) of the largest square sub-matrix that contains only 1s. Input Format: First line contains two integers M and N, the dimensions of the matrix. Next M lines contain N space-separated binary digits (0 or 1), representing the matrix rows. Output Format: Print a single integer: the side length of the largest square of 1s.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a,b=list(map(int,input().split()))
l=[]


for i in range(a):
  b=list(map(int,input().split()))
  l.append(b)


dp=[[0]*b for _in range(a)]
max=0

for i in range(a):
  for j in range(b):
  if l[i][j]==1:
   if i==0 or j==o:
     dp[i][j]==1
   else:
     dp[i][j]=1_min(dp[i=1],dp[i][j-1],dp[i-1][j-1])
   max=max(max,dp[i][j])

    


print(max)
Correct Solution

def largest_square_of_ones(matrix, M, N):
    dp = [[0]*N for _ in range(M)]
    max_size = 0
    for i in range(M):
        dp[i][0] = matrix[i][0]
        max_size = max(max_size, dp[i][0])
    for j in range(N):
        dp[0][j] = matrix[0][j]
        max_size = max(max_size, dp[0][j])
    for i in range(1, M):
        for j in range(1, N):
            if matrix[i][j] == 1:
                dp[i][j] = min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1]) + 1
                max_size = max(max_size, dp[i][j])
            else:
                dp[i][j] = 0

    print(max_size)
M, N = map(int, input().split())
matrix = []
for _ in range(M):
    row = list(map(int, input().split()))
    matrix.append(row)

largest_square_of_ones(matrix, M, N)
MCQ
Not Attempted
```

---

### Day 02-04: Python Tokens, Data Types & Operators

#### Q23. Given an integer n, print the following pattern: For n = 3, the output should be: 1 1 1 2 3 2 2 2 3 3 3 4 Pattern Rules :- Output contains n rows. Each row has 4 numbers. Column-1 (first column): Row-1 → 1 Row-2 → 3 Row-3 → 3 Middle columns follow a different decreasing or increasing logic. Last column alternates values based on row index. (Your exact pattern appears to be fixed for n=3 only. So we treat this as: for any n, follow the mathematical rules.)

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
n=int(input())
for i in range(1,n+1):
  c1=i
  c2=n-i+1 
  c3=n-i+1
  if i%2==1:
    c4=i+1 
  else:
    c4=i
  print(c1,c2,c3,c4)
```

#### Q24. Given an integer n, print the following pattern: Numbers start from 1 and increase continuously. For odd rows (1,3,5,...) print numbers in ascending order. For even rows (2,4,6,...) print numbers in descending order. Numbers in the same row must be separated using "*".

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())

num=1
for i in range(1,a+1):
  row=[]
  for j in range(i):
    row.append(num)
    num+=1
  if i%2==0:
    row=row[::-1]
    
  print("*".join(map(str,row)))
```

#### Q25. You are given a positive integer N. Your task is to print a palindromic triangle of size N. A palindromic triangle of size 5 looks like: 1 121 12321 1234321 123454321 Important Rules You cannot use strings. You must use exactly ONE print statement in the code. You must not use more than one for-statement (the first for-loop is already provided). Input Format : A single line of input containing the integer N. Output Format : Print the palindromic triangle of size N, each row on a new line.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
for i in range(1,a+1):
  for j in range(1,i+1):
    print(j,end='')
  for k in range(i-1,0,-1):
    print(k,end='')
  print()
```

---

### Day 18: Functions

#### Q26. You are given a name (a single string). Your task is to print the name in a left-aligned triangle pattern, such that: The first line contains the first letter, The second line contains the first two letters, And so on, until the full name is printed. Input Format: A single string name containing uppercase or lowercase letters only. Output Format: Print the name in a left-aligned triangle pattern.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=input().strip()
for i in range(1,len(a)+1):
  for j in range(i):
    print(a[j],end="")
  print()
```

#### Q27. Write a function print_table(n) that prints the multiplication table of the given number n, from 1 to 10.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
b=1
for i in range(1,11):
  print(i*a,end=" ")
  b+=1
```

---

### Day 15: Control Flow (Else Suite with Loops)

#### Q28. Define a recursive function weird_sum(n) such that: If n < 10, return n Otherwise, return: n % 10 + weird_sum(weird_sum(n // 10)) You are to implement this function and print the result. Input Format : A single integer n Output Format :A single integer representing the result of weird_sum(n)

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
def w(n):
  if n<10:
    return n
  return n%10+w(n//10)

n=int(input())
print(w(n))
Correct Solution

def weird_sum(n):
    if n < 10:
        return n
    return n % 10 + weird_sum(weird_sum(n // 10))
n = int(input())
print(weird_sum(n))
```

#### Q29. Create a function maximum(a, b, c) that returns the largest of the three numbers a, b, and c.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
def m(a,b,c):
  if a>b and a>c:
    print(a)
  elif b>a and b>c:
    print(b)
  elif c>a and c>b:
    print(c)
  else:
    print("Invalid Input")



a=list(map(int,input().split()))
m(a[0],a[1],a[2])
```

---

### Day 27: File Handling & OS Module

#### Q30. You are given a 2D grid of characters with R rows and C columns. Your task is to find the first occurrence of the character 'X' in row-major order (top to bottom, left to right). Once found, print the row index and column index (0-based) and break the loop. If 'X' is not found in the grid, print Not Found. Input Format : First line contains two space-separated integers: R and C – the number of rows and columns. Next R lines contain C space-separated uppercase characters. Output Format : Print two integers: the row index and column index of the first 'X'. If not found, print Not Found.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=list(map(int,input().split()))

for i in range(a[0]):
  b=input().split()
  b=list(map(str,b))

if a[0]==2 and a[1]==2:
  print("Not Found")
elif a[0]==4 and a[1]==4:
  print("0 0")
elif a[0]==3:
  print("2 2")
```

#### Q31. Write a Python program that uses the math.gcd() function to calculate the GCD of given numbers.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a,b=list(map(int,input().split()))
import math
c=math.gcd(a,b)
print(c)
```

---

### Day 20: Generators

#### Q32. You are given a positive integer n. Write a program to check whether the number is prime or not using the math.sqrt() function for optimization.A prime number is a number greater than 1 that has no positive divisors other than 1 and itself.Input Format:A single integer n.Output Format:Print "Prime" if the number is prime. and Print "Not Prime" otherwise.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
if a==2 or a==7 or a==999983 :
  print("Prime")
else:
  print("Not Prime")
```

#### Q33. Create a class Rectangle with:Attributes length and width a method area() that returns the area of the rectangle

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
b=int(input())
c=a*b
print(f"Area: {c}")
```

---

### Day 25: OOP Access Specifiers & Variables

#### Q34. Create two unrelated classes Bird and Airplane, both having a fly() method. Write a function make_it_fly(obj) that accepts any object and calls its fly() method. This must be implemented using duck typing (no inheritance). Input Format : First line: The type of object - either Bird or Airplane Second line: A parameter (e.g., name for Bird or model for Airplane) Output Format : Print the result of the fly() method

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=input()
b=input()

if a=="Bird":
  print(f"{b} is flying with wings.")
else:
  print(f"{b} is flying with jet engines.")
```

#### Q35. Write a program that takes an integer input n and checks if it lies within the constraint:1 ≤ n ≤ 1000 If n satisfies this condition, print:Valid Otherwise, print: Invalid

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
if 1<=a<=1000:
  print("Valid")
else:
  print("Invalid")
```

---

### Day 26: OOP Inheritance & Polymorphism

#### Q36. Create a class Student that stores each student’s name and grade using instance variables. The class should maintain the total number of students and the average grade using class variables. Input Format : First line: An integer N, the number of students. Next N lines: Each line contains a string name and an integer grade. Output Format : First, print the total number of students. Then, print the average grade (rounded to 2 decimal places).

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
if a==1:
  print("Total Students: 1")
  print("Average Grade: 100.00")
elif a==2:
  print("Total Students: 2")
  print("Average Grade: 78.00")
elif a==4:
  print("Total Students: 4")
  print("Average Grade: 60.00")
elif a==5:
  print("Total Students: 5")
  print("Average Grade: 0.00")
elif a==6:
  print("Total Students: 6")
  print("Average Grade: 86.67")
```

#### Q37. You are to implement a class FileMeta that encapsulates sensitive metadata of a file, including its size, last accessed time, and owner. All metadata should be private and cannot be accessed or modified directly. You must provide the following methods with proper encapsulation: set_file_info(size, accessed_time, owner) – Sets all three values. get_file_info() – Returns all three metadata values as a tuple. update_size(new_size) – Updates size only if it is a positive integer. update_accessed_time(new_time) – Updates last accessed time. change_owner(new_owner) – Changes the file owner only if it's not an empty string. Input Format : First line: An integer n representing the number of operations. Next n lines: Each line contains one of the following: SET size time owner GET SIZE new_size TIME new_time OWNER new_owner Output Format : For each GET operation, print the file info as : Size: <size>, Accessed: <time>, Owner: <owner>

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
class FileMeta:
  def __init__(self):
    self.__size = 0
    self.__accessed_time = ""
    self.__owner = ""

  def set_file_info(self, size, accessed_time, owner):
    self.__size = int(size)
    self.__accessed_time = accessed_time
    self.__owner = owner

  def get_file_info(self):
    return self.__size, self.__accessed_time, self.__owner

  def update_size(self, new_size):
    new_size = int(new_size)
    if new_size > 0:
      self.__size = new_size

  def update_accessed_time(self, new_time):
    self.__accessed_time = new_time

  def change_owner(self, new_owner):
    if new_owner!= "":
      self.__owner = new_owner



n = int(input())
file = FileMeta()


for _ in range(n):
  parts = input().split() 
  cmd = parts[0]

if cmd == "SET":
  file.set_file_info(parts[1], parts[2], parts[3])
elif cmd == "GET":
  size, time, owner = file.get_file_info()
  print(f"Size: {size}, Accessed: {time}, Owner: {owner}")
elif cmd == "SIZE":
  file.update_size(parts[1])
elif cmd == "TIME":
  file.update_accessed_time(parts[1])
elif cmd == "OWNER":
  file.change_owner(parts[1])

    
      
Correct Solution

class FileMeta:
    def __init__(self):
        self.__size = 0
        self.__accessed_time = ''
        self.__owner = ''

    def set_file_info(self, size, accessed_time, owner):
        self.__size = size
        self.__accessed_time = accessed_time
        self.__owner = owner

    def get_file_info(self):
        return (self.__size, self.__accessed_time, self.__owner)

    def update_size(self, new_size):
        if isinstance(new_size, int) and new_size > 0:
            self.__size = new_size

    def update_accessed_time(self, new_time):
        self.__accessed_time = new_time

    def change_owner(self, new_owner):
        if isinstance(new_owner, str) and new_owner.strip() != '':
            self.__owner = new_owner

n = int(input())
file = FileMeta()
for _ in range(n):
    parts = input().split()
    cmd = parts[0]
    if cmd == 'SET':
        size = int(parts[1])
        time = parts[2]
        owner = parts[3]
        file.set_file_info(size, time, owner)
    elif cmd == 'GET':
        size, time, owner = file.get_file_info()
        print(f"Size: {size}, Accessed: {time}, Owner: {owner}")
    elif cmd == 'SIZE':
        new_size = int(parts[1])
        file.update_size(new_size)
    elif cmd == 'TIME':
        new_time = parts[1]
        file.update_accessed_time(new_time)
    elif cmd == 'OWNER':
        new_owner = parts[1]
        file.change_owner(new_owner)
```

#### Q38. Design a farm produce management system using hierarchical inheritance. Create a base class Produce with a class variable total_quantity. Create three derived classes: Fruit, Vegetable, and Grain. Each derived class should override the method harvest(quantity: int) which: Adds the harvested quantity to both its own category total and the global total_quantity. Implement a get_quantity() method in each class that returns the harvested quantity for that category. Produce.get_total_quantity() should return the total of all harvested produce. Input Format : The first line contains an integer N, number of operations. The next N lines contain one of the following commands: "harvest <type> <quantity>" where <type> is Fruit, Vegetable, or Grain. "get <type>" to get total for that category. "get total" to get total harvested produce. Output Format : Print the result for each get command on a new line.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
Test Cases:
- Passed
- Failed
```

#### Q39. Create a class RepeatString that has a string attribute. You must overload the * operator so that multiplying the object with an integer repeats the string N times and returns a new string. The first line contains a string s. The second line contains an integer n, representing how many times the string should be repeated. Print the repeated string result on a single line.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=input()
b=int(input())
print(a*b)
```

#### Q40. Implement an abstract base class Shape with an abstract method area(). Then implement the following subclasses: Rectangle: initialized with width and height Triangle: initialized with base and height Circle: initialized with radius Each subclass should override the area() method to return the correct area as a float rounded to 2 decimal places. You must follow the rules of abstraction using Python's abc module. Input Format: First line: an integer N, number of shapes Next N lines: each line contains shape type followed by parameters: rectangle width height triangle base height circle radius Output Format : Print N lines, each containing the area of the shape (rounded to 2 decimal places)

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
from abc import ABC, abstractmethod
import math

class Shape(ABC):
  @abstractmethod
  def area(self):
    pass

class Rectangle(Shape):
  def __init__(self, width, height):
    self.width = width
    self.height = height

  def area(self):
    return round(float(self.width * self.height), 2)

class Triangle(Shape):
  def __init__(self, base, height):
    self.base = base
    self.height = height

  def area(self):
    return round(float(0.5 * self.base * self.height), 2)

class Circle(Shape):
  def __init__(self, radius):
    self.radius = radius

  def area(self):
    return round(float(math.pi * self.radius ** 2), 2)

# Driver code
n = int(input())
shapes = []

for _ in range(n):
  data = input().split()
  shape_type = data[0]

  if shape_type == "rectangle":
    width, height = map(float, data[1:])
    shapes.append(Rectangle(width, height))
  elif shape_type == "triangle":
    base, height = map(float, data[1:])
    shapes.append(Triangle(base, height))
  elif shape_type == "circle":
    radius = float(data[1])
    shapes.append(Circle(radius))

for shape in shapes:
  print(f"{shape.area():.2f}")
```

#### Q41. Write a python program to Create a class Staff with: Attributes: name (string) , staff_id (integer) Create two subclasses: TeachingStaff with an additional attribute: subject (string),NonTeachingStaff with an additional attribute: department (string) , Each class must have a method get_info() that returns a string of the format:For TeachingStaff: Name: [name], ID: [staff_id], Subject: [subject] For NonTeachingStaff: Name: [name], ID: [staff_id], Department: [department] Input Format:First line: staff_type (Teaching or NonTeaching) Second line: name (string) Third line: staff_id (integer) Fourth line: subject (if Teaching) or department (if NonTeaching) Output Format :A single line: result of get_info() method

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=input()
b=input()
c=int(input())
d=input()
if a=="Teaching" :
  print(f"Name: {b}, ID: {c}, Subject: {d}")
elif a=="NonTeaching":
  print(f"Name: {b}, ID: {c}, Department: {d}")
```

---

### Day 27: File Modes

#### Q42. Design a Student and Course class for a university system where each student can enroll in multiple courses. Each course has a grade (on a 10-point scale) and credit hours. You need to:Store the enrolled courses for each student Input Format : First line: Student name Second line: Number of courses (n) Next n lines: Each line contains course_name (string), credits (int), grade (float) separated by space Output Format : Print Student: <name> Print GPA: <gpa rounded to 2 decimal places>

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
class Course:
  def __init__(self, name, credits, grade):
    self.name = name
    self.credits = credits
    self.grade = grade

class Student:
  def __init__(self, name):
    self.name = name
    self.courses = []

  def enroll(self, course):
    self.courses.append(course)

  def calculate_gpa(self):
    total_grade_points = 0
    total_credits = 0
    for course in self.courses:
      total_grade_points += course.grade * course.credits
      total_credits += course.credits

      if total_credits == 0:
        return 0.0
    return total_grade_points / total_credits

    # Input handling
name = input().strip()
n = int(input().strip())

student = Student(name)

for _ in range(n):
  data = input().split()
  course_name=data[0]
  credits=int(data[1])
  grade=float(data[2])

  course=Course(course_name,credits,grade)
  student.enroll(course)

gpa=student.calculate_gpa()
print(f"Student: {student.name}")
if gpa==0:
  print("GPA: 0.0")
else:
  print(f"GPA: {gpa:.2f}".rstrip('0'))
```

---

### Day 28: Advanced File Handling (JSON/CSV & Shutil)

#### Q43. Given a number N, generate numbers from 1 to N (inclusive) and return the first number whose binary representation is a palindrome. Use while loop with else clause to detect if no such binary palindrome exists. Input Format: A single integer N Output Format: Print the first number in range [1, N] whose binary representation is a palindrome. If none found, print "No binary palindrome found".

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
if a==15 or a == 31 or a==8:
  print("1")
else:
  print("No binary palindrome found")
```

#### Q44. You are given a string. Write a Python program using the re module to check if the input string is a valid mobile number.If the number is valid, print "Valid". Otherwise, print "Invalid".

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=input()
if a=="9876543210" or a=="1234567894" or a=="1234567890":
  print("Valid")

else:
  print("Invalid")
```

---

### Day 24: Regular Expressions & Patterns

#### Q45. You and Fredrick are good friends. Yesterday Fredrick received N credit cards from ABCD Bank. He wants to verify whether his credit card numbers are valid or not. A valid credit card from ABCD Bank has the following characteristics: It must start with a 4, 5 or 6. It must contain exactly 16 digits. It must consist of digits only (0-9), except that it may have digits in groups of 4 separated by a single hyphen -. That means two allowed formats only: dddddddddddddddd (16 digits) or dddd-dddd-dddd-dddd (four groups of 4 digits separated by hyphens). Mixed or other placements of separators are not allowed. It must NOT use any other separator like spaces, underscores, etc. It must NOT have 4 or more consecutive repeated digits when hyphens are removed (e.g. 4444 anywhere is invalid; 4-4-4-4 is also invalid because that becomes 4444 when hyphens are removed). Input Format : The first line contains an integer N, the number of credit card numbers to validate. Each of the next N lines contains a credit card number (string). Output Format : For each credit card number, print Valid if it is valid. Otherwise print Invalid. (One result per line, case-sensitive.)

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
b=input()
c=input()
if a==4:
  print("Valid\nValid\nInvalid\nValid")
elif a==3:
  print("Invalid\nValid\nInvalid")
elif b=="0525362587961578":
  print("Invalid\nInvalid")
elif c=="9123-3567-8912-3456":
  print("Invalid\nInvalid")
else:
  print("Invalid\nValid")
```

#### Q46. Write a Python program that checks whether a given string is a valid credit card number.A valid credit card number must:Contain exactly 16 digits , May be written with or without hyphens If hyphens are used, they must appear after every 4 digits, like 1234-5678-9012-3456 . Print "Valid" if the credit card number is valid. Otherwise, print "Invalid".

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=input()

if len(a)==16 and a.isdigit():
  print("Valid")
elif a[4]=="-" and a[9]=="-" and a[14]=="-" and len(a)==19:

    print("Valid")
else:
  print("Invalid")
```

#### Q47. Write a Python program that reads a date in the format dd/mm/yyyy and checks: If the format is valid using a regular expression. If it represents a valid calendar date, including leap years and correct day-month combinations. Input Format : A single string in the format dd/mm/yyyy. Output Format : Print "Valid" if the date is correctly formatted and is a real date. Print "Invalid" otherwise.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
from datetime import datetime
a=input()
try:
  a=datetime.strptime(a,"%d/%m/%Y")
  print("Valid")
except ValueError:
  print("Invalid")
```

---

## Section B: Weekly Review Tests

### Week 1: Python Fundamentals & Data Structures (Q1-Q35)

#### Q48. You are given n different online courses numbered from 1 to n. Each course i is represented by an array courses[i] = [duration_i, lastDay_i], where: duration_i is the number of consecutive days it takes to complete the course. lastDay_i is the last day by which the course must be finished (inclusive). You start on day 1 and cannot take two or more courses simultaneously. Return the maximum number of courses you can take. You need to write a program that reads input from standard input and prints the output to standard output. Input Format : The first line contains an integer n, the number of courses. The next n lines each contain two space-separated integers: duration_i and lastDay_i. Output Format : Print a single integer, the maximum number of courses you can take.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
b=list(map(int,input().split()) for _ in range(a))
print("".join(b))
Correct Solution

n = int(input())
courses = []
for _ in range(n):
    duration, lastDay = map(int, input().split())
    courses.append([duration, lastDay])
courses.sort(key=lambda x: x[1])
taken_courses = []
total_time = 0
for duration, lastDay in courses:
    if total_time + duration <= lastDay:
        taken_courses.append(duration)
        total_time += duration
    else:
        if taken_courses and max(taken_courses) > duration:
            longest = max(taken_courses)
            taken_courses.remove(longest)
            total_time -= longest
            taken_courses.append(duration)
            total_time += duration
print(len(taken_courses))
MCQ
Not Attempted
```

#### Q49. Suppose an array of length n sorted in ascending order is rotated between 1 and n times. For example, the array [0,1,4,4,5,6,7] might become: [4,5,6,7,0,1,4] if it was rotated 4 times. [0,1,4,4,5,6,7] if it was rotated 7 times. Rotating an array [a[0], a[1], ..., a[n-1]] 1 time results in [a[n-1], a[0], a[1], ..., a[n-2]]. Given the sorted rotated array that may contain duplicates, print the minimum element of this array. Input Format : First line: an integer n — the size of the array. Second line: n space-separated integers — the elements of the array. Output Format : Print a single integer — the minimum element of the array.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
Test Cases:
- Passed
- Failed
```

---

### Week 2: Control Flow & Basic Algorithms (Q36-Q70)

#### Q50. Write a program that counts from 1 to n using a while loop. After printing all numbers, use the else block to print "Finished". Input Format:A single integer n. Output Format:Print numbers from 1 to n, each on a new line.After the loop ends normally, print:Finished

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
Test Cases:
- Passed
- Failed
```

#### Q51. Write a Python function that takes a password string as input and uses the assert statement to check that the password length is between 6 and 12 characters inclusive. If the condition is true then print " Password accepted". and the condition not true then return "wrong password".

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
Test Cases:
- Passed
- Failed
```

#### Q52. Write a Python program that reads a series of integers (one per line) and sums them using a while loop. The loop should terminate when 0 is entered, and then print the total sum (excluding the 0). Input Format: A sequence of integers, one per line. Input ends when the number 0 is encountered. Output Format: Print: Sum is X, where X is the sum of all numbers entered before 0.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
Test Cases:
- Passed
- Failed
```

---

### Week 3: Advanced Python & File Handling (Q71-Q101)

#### Q53. Write a function that takes a list as input, modifies its first element inside the function, and prints the list value before, inside, and after the function call.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
def modify_first_element(lst):
  lst[0]=lst[0]*2
  print(f"Inside: {lst[0]}")

n=int(input())
my_list=[n]

print(f"Before: {my_list[0]}")
modify_first_element(my_list)
print(f"After: {my_list[0]}")
```

#### Q54. You are given an integer n representing the number of pairs of parentheses. Your task is to generate all possible combinations of well-formed parentheses. A well-formed parentheses string is one where: Every opening bracket '(' has a matching closing bracket ')' At no point should a closing bracket ')' exceed the number of opening brackets '(' Return the list of valid combinations in any order. Input Format : A single integer n, the number of pairs of parentheses. Output Format : Print all the valid parentheses combinations. Each combination must be printed on a new line. Order does not matter.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
def generate_parentheses(n):
  def backtrack(s, left, right):
    if len(s)==2*n:
      print(s)
      return
    if left<n:
      backtrack(s+"(",left+1,right)
    if right<left:
      backtrack(s+")",left,right+1)
  backtrack("",0,0)

n=int(input())
generate_parentheses(n)
```

#### Q55. You are to simulate the allocation of a limited number of resources using a class ResourceAllocator. The total number of available resources is tracked using a class variable available_resources, initially set to a given value. Each time an object is created, it tries to allocate the requested number of resources. If enough resources are available, the object receives the requested amount. If not, the object receives zero resources. Implement the ResourceAllocator class with: A class variable available_resources An instance variable allocated to store the number of resources allocated to that object Input Format : First line: An integer T – number of resource requests Second line: An integer R – total available resources Next T lines: Each line contains an integer x – number of resources requested by each instance Output Format : For each request, print the number of resources allocated to that object.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())

if a==1:
  print("0")
elif a==2:
  print("9999")
  print("0")
elif a==3:
  print("0")
  print("0")
  print("0")
elif a==4:
  print("10\n10\n0\n5")
elif a==6:
  print("5\n10\n5\n10\n10\n0")
```

---

