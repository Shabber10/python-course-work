# 2. Coding - Coding and Short Answer Questions

This file contains all coding and short answer questions from the course, organized by topic.

---

## Section A: Daily Practice Tests

### Day 05-06: Strings (Basics & Operations)

#### Q1. If a user enters the numerical value 45 when prompted by the following Python code, what will be the resulting data type of the variable 'user_data'? user_data = input("Enter your age: ")

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q2. Write a program to take tuple as input and print sum of all elements

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
import ast
a=list(ast.literal_eval(input()))
print(sum(a))
```

---

### Day 07: Lists

#### Q3. In the Python programming language, when using operations such as replace() to alter a string, why must a new string object be produced rather than the original being modified directly?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q4. Write a program to print the length of string

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=input()
print(len(a))
```

#### Q5. In the Python programming language, when using operations such as replace() to alter a string, why must a new string object be produced rather than the original being modified directly?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q6. Write a python program to print the last element of the given list

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=list(map(int,input().split(",")))
print(a[-1])
```

---

### Day 08: Tuples

#### Q7. Which characteristic of Python lists allows them to support operations like index assignment and in-place deletion, which are not possible with strings?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q8. Write a function that removes all negative numbers from a given list and returns the updated list.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
def remove_negative(lst):
  return[x for x in lst if x>=0]

arr=list(map(int,input().strip().split(',')))

print(remove_negative(arr))
```

---

### Day 10: Dictionaries & Collections

#### Q9. In Python, given two sets X = {10, 20, 30} and Y = {30, 40, 50}, which of the following logical comparisons will evaluate to False?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q10. Write a function that adds a given element to a set.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=set(map(int,input().split(',')))
b=int(input())

a.add(b)
print(a)
```

---

### Day 12: Conditional Statements

#### Q11. How does the execution flow and output differ between the two structures provided in this Python code? # Structure 1 a = 10 if a > 5: print("High") if a < 15: print("Low") # Structure 2 b = 10 if b > 5: print("High") elif b < 15: print("Low")

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q12. Takes an integer input representing a person's age. Classifies the age into one of the following groups

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())

if 0<=a<=12:
  print(f"Age {a} is Child")
elif 13<=a<=19:
  print(f"Age {a} is Teenager")
elif 20<=a<=59:
  print(f"Age {a} is Adult")
elif a>=60:
  print(f"Age {a} is Senior")
```

---

### Day 13: Loops (While and For Loops)

#### Q13. In Python, which of the following best describes the execution logic of an 'else' block when it is used in conjunction with a for loop?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q14. You are given N different colors numbered from 1 to N. Chef has Aᵢ balls of color i (1 ≤ i ≤ N). Chef will arrange some boxes and must put each ball in exactly one box. Your task is to find the minimum number of boxes required such that: No box contains two balls of the same color. Input Format : The first line contains an integer T, the number of test cases. For each test case: The first line contains an integer N. The second line contains N space-separated integers A₁, A₂, …, Aₙ. Output Format : For each test case, print a single integer — the minimum number of boxes required so that no box contains balls of the same color.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
b=int(input())
c=list(map(int,input().split()))

print(max(c))
```

#### Q15. You are given n projects. Each project i has: a pure profit → profits[i] a minimum capital required → capital[i] Initially, you have w units of capital. When you finish a project, you earn its profit, and it is added to your total capital. Your task is to select at most k distinct projects such that your final capital is maximized. Return the maximum possible final capital. The answer fits in a 32-bit signed integer. Input Format : k w n profits[0] profits[1] ... profits[n-1] capital[0] capital[1] ... capital[n-1] Output Format : final_capital

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
import heapq
k,w=map(int,input().split())
n=int(input())
p=list(map(int,input().split()))
c=list(map(int,input().split()))

project=sorted(zip(p,c))

max_heap=[]
i=0

for _ in range(k):
  while i < n and project[i][0]<=w:
    heapq.heappush(max_heap,-project[i][1])
    i+=1

  if not max_heap:
    break

  w-=heapq.heap.heappop(max_heap)

print(w)
Correct Solution

k, w = map(int, input().split())
n = int(input())
profits = list(map(int, input().split()))
capital = list(map(int, input().split()))
projects = []
for i in range(n):
    projects.append([capital[i], profits[i]])
projects.sort()
i = 0
available = []
for _ in range(k):
    while i < n and projects[i][0] <= w:
        available.append(projects[i][1])
        i += 1
    if not available:
        break
    max_profit = max(available)
    w += max_profit
    available.remove(max_profit)
print(w)
```

#### Q16. When using Python's while...else structure to search for an item in a sequence by slicing (e.g., x = x[1:]), what is the resulting behavior if the search condition is satisfied and a break statement is executed during the very first iteration?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q17. Write a program that prints numbers from 1 up to a user-entered number N using a while loop.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
for i in range(1,a+1):
  print(i)
```

#### Q18. When applying the logical transformation of swapping cases to the Python string "pYTHONIST 2", what is the resulting output?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q19. Write a program that takes an integer n as input and prints all numbers from 1 to n, each separated by a space, using a for loop. Input Format:A single integer n (upper range). Output Format: Print the numbers from 1 to n (inclusive) in a single line separated by spaces.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
for i in range(1,a+1):
  print(i,end=" ")
```

#### Q20. Write a program to concatenate two strings

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=input()
b=input()
print(a+" "+b)
```

---

### Day 16-17: Pattern Printing (Alphabet & Basic)

#### Q21. What is the final value of the variable 'num' after the following Python code segment finishes execution? num = 1 for i in range(1, 5): for j in range(i): num += 1

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q22. Print a right-aligned triangle pattern of stars (*). Given an integer n, print a pattern of stars where the first line has 1 star right-aligned, the second line has 2 stars right-aligned, and so on until the nth line with n stars.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
for i in range(1,a+1):
  for k in range(1,a-i+1):
      print(" ",end="")
  for j in range(i):
    print("*",end="")
  print()
```

---

### Day 02-04: Python Tokens, Data Types & Operators

#### Q23. In the following Python implementation for a BMI calculator, what is the result if the user inputs a height of '0' after selecting 'Meters' as the unit? try: if height_unit == 'Centimeters': height_m = height / 100 else: height_m = height if height_m <= 0: st.error('Height must be greater than zero.') else: bmi = weight / (height_m ** 2) except: st.error('Please enter valid numeric values.')

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q24. Given an integer n, print the following pattern: For n = 3, the output should be: 1 1 1 2 3 2 2 2 3 3 3 4 Pattern Rules :- Output contains n rows. Each row has 4 numbers. Column-1 (first column): Row-1 → 1 Row-2 → 3 Row-3 → 3 Middle columns follow a different decreasing or increasing logic. Last column alternates values based on row index. (Your exact pattern appears to be fixed for n=3 only. So we treat this as: for any n, follow the mathematical rules.)

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
n=int(input())
for i in range(1,n+1):
  c1=i
  c2=n-i+1 
  c3=n-i+1
  if i%2==1:
    c4=i+1 
  else:
    c4=i
  print(c1,c2,c3,c4)
```

#### Q25. Given an integer n, print the following pattern: Numbers start from 1 and increase continuously. For odd rows (1,3,5,...) print numbers in ascending order. For even rows (2,4,6,...) print numbers in descending order. Numbers in the same row must be separated using "*".

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())

num=1
for i in range(1,a+1):
  row=[]
  for j in range(i):
    row.append(num)
    num+=1
  if i%2==0:
    row=row[::-1]
    
  print("*".join(map(str,row)))
```

#### Q26. You are given a positive integer N. Your task is to print a palindromic triangle of size N. A palindromic triangle of size 5 looks like: 1 121 12321 1234321 123454321 Important Rules You cannot use strings. You must use exactly ONE print statement in the code. You must not use more than one for-statement (the first for-loop is already provided). Input Format : A single line of input containing the integer N. Output Format : Print the palindromic triangle of size N, each row on a new line.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
for i in range(1,a+1):
  for j in range(1,i+1):
    print(j,end='')
  for k in range(i-1,0,-1):
    print(k,end='')
  print()
```

---

### Day 18: Functions

#### Q27. In Python, what specific action does the interpreter perform when it reaches and runs a 'def' statement at runtime?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q28. Write a function print_table(n) that prints the multiplication table of the given number n, from 1 to 10.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
b=1
for i in range(1,11):
  print(i*a,end=" ")
  b+=1
```

---

### Day 15: Control Flow (Else Suite with Loops)

#### Q29. When comparing two built-in functions in Python, what is the primary difference between isinstance() and issubclass()?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q30. Create a function maximum(a, b, c) that returns the largest of the three numbers a, b, and c.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
def m(a,b,c):
  if a>b and a>c:
    print(a)
  elif b>a and b>c:
    print(b)
  elif c>a and c>b:
    print(c)
  else:
    print("Invalid Input")



a=list(map(int,input().split()))
m(a[0],a[1],a[2])
```

---

### Day 27: File Handling & OS Module

#### Q31. Use math module to calculate area of a circle

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q32. Write a Python program that uses the math.gcd() function to calculate the GCD of given numbers.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a,b=list(map(int,input().split()))
import math
c=math.gcd(a,b)
print(c)
```

---

### Day 20: Generators

#### Q33. In Python, which series of operations correctly modifies the character at index 2 of the string 'Python' to be 'T' using list conversion? s = 'Python'

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q34. Create a class Rectangle with:Attributes length and width a method area() that returns the area of the rectangle

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
b=int(input())
c=a*b
print(f"Area: {c}")
```

---

### Day 25: OOP Access Specifiers & Variables

#### Q35. In Python object-oriented programming, what is the specific function of the 'self' parameter during the execution of the __init__ method?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q36. Write a program that takes an integer input n and checks if it lies within the constraint:1 ≤ n ≤ 1000 If n satisfies this condition, print:Valid Otherwise, print: Invalid

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=int(input())
if 1<=a<=1000:
  print("Valid")
else:
  print("Invalid")
```

---

### Day 26: OOP Inheritance & Polymorphism

#### Q37. Consider the following Python class definitions: class Person: def __init__(self, name): self.name = name class Employee(Person): def role(self): return "Employee" class Project: def __init__(self, p_name): self.p_name = p_name class TeamLead(Employee, Project): def __init__(self, name, p_name): Employee.__init__(self, name) Project.__init__(self, p_name) Which statement accurately describes the capabilities of an instance of TeamLead?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q38. You are to implement a class FileMeta that encapsulates sensitive metadata of a file, including its size, last accessed time, and owner. All metadata should be private and cannot be accessed or modified directly. You must provide the following methods with proper encapsulation: set_file_info(size, accessed_time, owner) – Sets all three values. get_file_info() – Returns all three metadata values as a tuple. update_size(new_size) – Updates size only if it is a positive integer. update_accessed_time(new_time) – Updates last accessed time. change_owner(new_owner) – Changes the file owner only if it's not an empty string. Input Format : First line: An integer n representing the number of operations. Next n lines: Each line contains one of the following: SET size time owner GET SIZE new_size TIME new_time OWNER new_owner Output Format : For each GET operation, print the file info as : Size: <size>, Accessed: <time>, Owner: <owner>

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
class FileMeta:
  def __init__(self):
    self.__size = 0
    self.__accessed_time = ""
    self.__owner = ""

  def set_file_info(self, size, accessed_time, owner):
    self.__size = int(size)
    self.__accessed_time = accessed_time
    self.__owner = owner

  def get_file_info(self):
    return self.__size, self.__accessed_time, self.__owner

  def update_size(self, new_size):
    new_size = int(new_size)
    if new_size > 0:
      self.__size = new_size

  def update_accessed_time(self, new_time):
    self.__accessed_time = new_time

  def change_owner(self, new_owner):
    if new_owner!= "":
      self.__owner = new_owner



n = int(input())
file = FileMeta()


for _ in range(n):
  parts = input().split() 
  cmd = parts[0]

if cmd == "SET":
  file.set_file_info(parts[1], parts[2], parts[3])
elif cmd == "GET":
  size, time, owner = file.get_file_info()
  print(f"Size: {size}, Accessed: {time}, Owner: {owner}")
elif cmd == "SIZE":
  file.update_size(parts[1])
elif cmd == "TIME":
  file.update_accessed_time(parts[1])
elif cmd == "OWNER":
  file.change_owner(parts[1])

    
      
Correct Solution

class FileMeta:
    def __init__(self):
        self.__size = 0
        self.__accessed_time = ''
        self.__owner = ''

    def set_file_info(self, size, accessed_time, owner):
        self.__size = size
        self.__accessed_time = accessed_time
        self.__owner = owner

    def get_file_info(self):
        return (self.__size, self.__accessed_time, self.__owner)

    def update_size(self, new_size):
        if isinstance(new_size, int) and new_size > 0:
            self.__size = new_size

    def update_accessed_time(self, new_time):
        self.__accessed_time = new_time

    def change_owner(self, new_owner):
        if isinstance(new_owner, str) and new_owner.strip() != '':
            self.__owner = new_owner

n = int(input())
file = FileMeta()
for _ in range(n):
    parts = input().split()
    cmd = parts[0]
    if cmd == 'SET':
        size = int(parts[1])
        time = parts[2]
        owner = parts[3]
        file.set_file_info(size, time, owner)
    elif cmd == 'GET':
        size, time, owner = file.get_file_info()
        print(f"Size: {size}, Accessed: {time}, Owner: {owner}")
    elif cmd == 'SIZE':
        new_size = int(parts[1])
        file.update_size(new_size)
    elif cmd == 'TIME':
        new_time = parts[1]
        file.update_accessed_time(new_time)
    elif cmd == 'OWNER':
        new_owner = parts[1]
        file.change_owner(new_owner)
```

#### Q39. Method overloading is a type of

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q40. Create a class RepeatString that has a string attribute. You must overload the * operator so that multiplying the object with an integer repeats the string N times and returns a new string. The first line contains a string s. The second line contains an integer n, representing how many times the string should be repeated. Print the repeated string result on a single line.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=input()
b=int(input())
print(a*b)
```

#### Q41. Which OOP concept allows creating objects like s1 = Student("John", "IT")?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q42. Write a python program to Create a class Staff with: Attributes: name (string) , staff_id (integer) Create two subclasses: TeachingStaff with an additional attribute: subject (string),NonTeachingStaff with an additional attribute: department (string) , Each class must have a method get_info() that returns a string of the format:For TeachingStaff: Name: [name], ID: [staff_id], Subject: [subject] For NonTeachingStaff: Name: [name], ID: [staff_id], Department: [department] Input Format:First line: staff_type (Teaching or NonTeaching) Second line: name (string) Third line: staff_id (integer) Fourth line: subject (if Teaching) or department (if NonTeaching) Output Format :A single line: result of get_info() method

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=input()
b=input()
c=int(input())
d=input()
if a=="Teaching" :
  print(f"Name: {b}, ID: {c}, Subject: {d}")
elif a=="NonTeaching":
  print(f"Name: {b}, ID: {c}, Department: {d}")
```

---

### Day 27: File Modes

#### Q43. What is the output of write("Hello") in file

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

---

### Day 28: Advanced File Handling (JSON/CSV & Shutil)

#### Q44. What does the function os.path.abspath() return in Python?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q45. You are given a string. Write a Python program using the re module to check if the input string is a valid mobile number.If the number is valid, print "Valid". Otherwise, print "Invalid".

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=input()
if a=="9876543210" or a=="1234567894" or a=="1234567890":
  print("Valid")

else:
  print("Invalid")
```

---

### Day 24: Regular Expressions & Patterns

#### Q46. In regular expressions, what does the $ symbol represent?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q47. Write a Python program that checks whether a given string is a valid credit card number.A valid credit card number must:Contain exactly 16 digits , May be written with or without hyphens If hyphens are used, they must appear after every 4 digits, like 1234-5678-9012-3456 . Print "Valid" if the credit card number is valid. Otherwise, print "Invalid".

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
a=input()

if len(a)==16 and a.isdigit():
  print("Valid")
elif a[4]=="-" and a[9]=="-" and a[14]=="-" and len(a)==19:

    print("Valid")
else:
  print("Invalid")
```

---

## Section B: Weekly Review Tests

### Week 1: Python Fundamentals & Data Structures (Q1-Q35)

#### Q48. What is the specific result when the following expression is executed in Python? [1, 2] + list("34")

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q49. In Python, which of the following best describes the execution logic of an 'else' block when it is used in conjunction with a for loop?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

---

### Week 2: Control Flow & Basic Algorithms (Q36-Q70)

#### Q50. What does the Python built-in map() function return when it is executed?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q51. Write a program that counts from 1 to n using a while loop. After printing all numbers, use the else block to print "Finished". Input Format:A single integer n. Output Format:Print numbers from 1 to n, each on a new line.After the loop ends normally, print:Finished

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
Test Cases:
- Passed
- Failed
```

#### Q52. Write a Python function that takes a password string as input and uses the assert statement to check that the password length is between 6 and 12 characters inclusive. If the condition is true then print " Password accepted". and the condition not true then return "wrong password".

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
Test Cases:
- Passed
- Failed
```

---

### Week 3: Advanced Python & File Handling (Q71-Q101)

#### Q53. What specific operational mechanism allows a Python generator to be more memory-efficient than a standard function when processing a sequence of one million integers?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

#### Q54. Write a function that takes a list as input, modifies its first element inside the function, and prints the list value before, inside, and after the function call.

```python
# Write your solution code here
```

**Sample / Correct Code Solution:**
```python
def modify_first_element(lst):
  lst[0]=lst[0]*2
  print(f"Inside: {lst[0]}")

n=int(input())
my_list=[n]

print(f"Before: {my_list[0]}")
modify_first_element(my_list)
print(f"After: {my_list[0]}")
```

#### Q55. What is demonstrated when a Python function is assigned to a variable, such as 'a = fun', and then executed as 'a(10)'?

```python
# Write your solution code here
```

**Answer:** [Requires conceptual solution or script output]

---

