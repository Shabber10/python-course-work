# 3. Sequences (Lists, Tuples, Sets, Dictionaries)

Python has four built-in data types used to store collections of data (often referred to under the umbrella of sequences and collections). Here is a detailed guide for Lists, Tuples, Sets, and Dictionaries.

---

## 1. Lists
Lists are used to store multiple items in a single variable.
- **Characteristics:**
  * **Ordered:** The items have a defined order, and that order will not change.
  * **Changeable (Mutable):** We can change, add, and remove items in a list after it has been created.
  * **Allow Duplicates:** Since lists are indexed, lists can have items with the same value.
  * **Syntax:** Written with square brackets: `[]`

**Input:**
```python
# Creating a list
fruits = ["apple", "banana", "cherry", "apple"]
print("List:", fruits)

# Accessing an item
print("First fruit:", fruits[0])

# Changing an item
fruits[1] = "blueberry"

# Adding an item
fruits.append("orange")
print("Modified List:", fruits)
```
**Output:**
```text
List: ['apple', 'banana', 'cherry', 'apple']
First fruit: apple
Modified List: ['apple', 'blueberry', 'cherry', 'apple', 'orange']
```

---

## 2. Tuples
Tuples are used to store multiple items in a single variable.
- **Characteristics:**
  * **Ordered:** The items have a defined order, and that order will not change.
  * **Unchangeable (Immutable):** We cannot change, add, or remove items after the tuple has been created.
  * **Allow Duplicates:** Like lists, tuples can have duplicate values.
  * **Syntax:** Written with round brackets: `()`

**Input:**
```python
# Creating a tuple
coordinates = (10.0, 20.0, 10.0)
print("Tuple:", coordinates)

# Accessing an item
print("X-Coordinate:", coordinates[0])

# coordinates[0] = 15.0 # THIS WOULD CAUSE AN ERROR! Tuples are immutable.
```
**Output:**
```text
Tuple: (10.0, 20.0, 10.0)
X-Coordinate: 10.0
```

---

## 3. Sets
Sets are used to store multiple items in a single variable.
- **Characteristics:**
  * **Unordered:** The items do not have a defined order. You cannot be sure in which order the items will appear.
  * **Unchangeable (Elements):** You cannot change individual items, but you can add new items and remove existing ones.
  * **No Duplicates:** Every element in a set must be unique. Duplicate values are automatically removed.
  * **Unindexed:** You cannot access items in a set by referring to an index.
  * **Syntax:** Written with curly brackets: `{}`

**Input:**
```python
# Creating a set (duplicates are ignored)
unique_numbers = {1, 2, 3, 3, 4}
print("Set (duplicates removed):", unique_numbers)

# Adding an item
unique_numbers.add(5)

# Removing an item
unique_numbers.remove(2)
print("Modified Set:", unique_numbers)
```
**Output:**
```text
Set (duplicates removed): {1, 2, 3, 4}
Modified Set: {1, 3, 4, 5}
```

---

## 4. Dictionaries
Dictionaries are used to store data values in `key:value` pairs.
- **Characteristics:**
  * **Ordered:** The items have a defined order (as of Python 3.7).
  * **Changeable (Mutable):** We can change, add, or remove items after the dictionary has been created.
  * **No Duplicates in Keys:** Keys must be unique; duplicate keys will overwrite existing values.
  * **Syntax:** Written with curly brackets and key:value pairs: `{key: value}`

**Input:**
```python
# Creating a dictionary
car = {
    "brand": "Ford",
    "model": "Mustang",
    "year": 1964
}
print("Dictionary:", car)

# Accessing by key
print("Car Model:", car["model"])

# Modifying a value
car["year"] = 2020

# Adding a new key-value pair
car["color"] = "red"
print("Modified Dictionary:", car)
```
**Output:**
```text
Dictionary: {'brand': 'Ford', 'model': 'Mustang', 'year': 1964}
Car Model: Mustang
Modified Dictionary: {'brand': 'Ford', 'model': 'Mustang', 'year': 2020, 'color': 'red'}
```

---

## 5. Summary Cheat Sheet

| Data Type  | Ordered | Changeable (Mutable) | Duplicates Allowed | Syntax |
|------------|---------|----------------------|--------------------|--------|
| **List**       | Yes     | Yes                  | Yes                | `[]`     |
| **Tuple**      | Yes     | No                   | Yes                | `()`     |
| **Set**        | No      | No (Items can't be changed, only added) | No | `{}`     |
| **Dictionary** | Yes     | Yes                  | No (Keys)          | `{k:v}`  |

<br>
<h3 align="center">
  <a href="2.%20Type%20Conversion%20and%20Casting.md"><kbd style="font-size: 20px; padding: 5px 10px;">&nbsp;⏮️ Prev&nbsp;</kbd></a>
  &nbsp;&nbsp;&nbsp;&nbsp;
  <a href="../DAY-04%20Operators/1.%20Unary%20and%20Ternary%20Operators.md"><kbd style="font-size: 20px; padding: 5px 10px;">&nbsp;Next ⏭️&nbsp;</kbd></a>
</h3>
