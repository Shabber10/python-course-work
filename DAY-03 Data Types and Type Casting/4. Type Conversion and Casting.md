# 5. Type Conversion and Casting

There may be times when you want to specify a type on to a variable. This can be done with casting. Python is an object-orientated language, and as such it uses classes to define data types, including its primitive types.

Casting in python is therefore done using constructor functions:

---

## 1. Explicit Type Casting

### `int()`
Constructs an integer number from an integer literal, a float literal (by removing all decimals), or a string literal (providing the string represents a whole number).
**Input:**
```python
x = int(1)   # x will be 1
y = int(2.8) # y will be 2
z = int("3") # z will be 3
print(x, y, z)
```
**Output:**
```text
1 2 3
```

### `float()`
Constructs a float number from an integer literal, a float literal, or a string literal (providing the string represents a float or an integer).
**Input:**
```python
x = float(1)     # x will be 1.0
y = float(2.8)   # y will be 2.8
z = float("3")   # z will be 3.0
w = float("4.2") # w will be 4.2
print(x, y, z, w)
```
**Output:**
```text
1.0 2.8 3.0 4.2
```

### `str()`
Constructs a string from a wide variety of data types, including strings, integer literals, and float literals.
**Input:**
```python
x = str("s1") # x will be 's1'
y = str(2)    # y will be '2'
z = str(3.0)  # z will be '3.0'
print(x, y, z)
```
**Output:**
```text
s1 2 3.0
```

<br>
<h3 align="center">
  <a href="3.%20Getting%20Data%20Type%20(type%20function).md"><kbd style="font-size: 20px; padding: 5px 10px;">&nbsp;⏮️ Prev&nbsp;</kbd></a>
  &nbsp;&nbsp;&nbsp;&nbsp;
  <a href="../DAY-04%20Operators/1.%20Unary%20and%20Ternary%20Operators.md"><kbd style="font-size: 20px; padding: 5px 10px;">&nbsp;Next ⏭️&nbsp;</kbd></a>
</h3>
