# String Concatenation and Formatting

Often, you will need to combine multiple strings together, or insert variables directly into a string. Python provides several ways to do this, ranging from basic addition to advanced formatting techniques.

---

## 6. String Concatenation

To combine two or more strings, you can use the `+` operator. This process is called **Concatenation**.

**Input:**
```python
a = "Hello"
b = "World"
c = a + b

print(c)
```
**Output:**
```text
HelloWorld
```

### Adding Spaces
Notice that the `+` operator does NOT automatically add a space between the words. If you want a space, you must manually concatenate a space string `" "`.

**Input:**
```python
a = "Hello"
b = "World"
c = a + " " + b

print(c)
```
**Output:**
```text
Hello World
```

---

## 7. String Formatting (The `.format()` Method)

We CANNOT combine strings and numbers together using the `+` operator. Python will throw a `TypeError`!

**Input:**
```python
age = 36
# txt = "My name is John, I am " + age  # THIS WILL CRASH!
```

To combine strings and numbers, we use the `format()` method! The `format()` method takes the passed arguments, formats them, and places them in the string where the placeholders `{}` are.

**Input:**
```python
age = 36
txt = "My name is John, and I am {} years old."
print(txt.format(age))
```
**Output:**
```text
My name is John, and I am 36 years old.
```

### Formatting with Specific Units
You can format numbers alongside units using the `.format()` method:
**Input:**
```python
n = 10
formatted_str = "{0} units".format(n)
print("Formatted Units:", formatted_str)
```
**Output:**
```text
Formatted Units: 10 units
```

---

### Multiple Placeholders
The `format()` method can take an unlimited number of arguments, and they are placed into the respective placeholders sequentially.

**Input:**
```python
quantity = 3
item_no = 567
price = 49.95
my_order = "I want {} pieces of item {} for {} dollars."

print(my_order.format(quantity, item_no, price))
```
**Output:**
```text
I want 3 pieces of item 567 for 49.95 dollars.
```

### Indexed Placeholders
You can use index numbers `{0}` to be sure the arguments are placed in the correct placeholders, even if you want to use the same argument multiple times!

**Input:**
```python
quantity = 3
item_no = 567
price = 49.95
my_order = "I want to pay {2} dollars for {0} pieces of item {1}."

print(my_order.format(quantity, item_no, price))
```
**Output:**
```text
I want to pay 49.95 dollars for 3 pieces of item 567.
```

---

## 8. f-Strings (Python 3.6+)

As of Python version 3.6, the absolute best, most modern way to format strings is using **f-Strings**. 

By putting an `f` before the first quote, you can put variables *directly inside* the curly braces `{}` without needing to use `.format()`. It is incredibly clean and readable!

**Input:**
```python
name = "Alice"
age = 25
score = 98.5

# Notice the 'f' at the very beginning of the string!
profile = f"Student Name: {name}, Age: {age}, Score: {score}"
print(profile)

# You can even do math directly inside the curly braces!
print(f"In 5 years, {name} will be {age + 5} years old.")
```
**Output:**
```text
Student Name: Alice, Age: 25, Score: 98.5
In 5 years, Alice will be 30 years old.
```

### A. Advanced f-String Formatting Capabilities
f-Strings are not just for basic variable interpolation; they support powerful formatting specifications directly within the curly braces `{}` using a colon `:` syntax.

**Input:**
```python
import datetime

# 1. Floating-point precision (.2f rounds to 2 decimal places)
pi = 3.14159265
print(f"Pi to 2 decimal places: {pi:.2f}")
print(f"Pi to 4 decimal places: {pi:.4f}")

# 2. Alignment and Padding (e.g., width of 10 characters)
number = 42
print(f"Left-aligned  : '{number:<10}'")
print(f"Right-aligned : '{number:>10}'")
print(f"Center-aligned: '{number:^10}'")
print(f"Padded with zeros: '{number:05}'")

# 3. Formatting Date and Time
today = datetime.date(2026, 6, 30)
print(f"Today's Date: {today:%B %d, %Y}")

# 4. Self-Documenting/Debug helper (x= prints variable name and its value) - Python 3.8+
x = 100
y = 200
print(f"Debugging variables: {x=}, {y=}")
```
**Output:**
```text
Pi to 2 decimal places: 3.14
Pi to 4 decimal places: 3.1416
Left-aligned  : '42        '
Right-aligned : '        42'
Center-aligned: '    42    '
Padded with zeros: '00042'
Today's Date: June 30, 2026
Debugging variables: x=100, y=200
```

### B. Evolution of String Interpolation (Comparison)
Python's string formatting has gone through three main stages. Here is how they compare in action:

| Style | Syntax | Performance | Pros / Cons |
| :--- | :--- | :--- | :--- |
| **Old-School `%` Formatting** | `"%s is %d" % ("Age", 30)` | Slowest | Traditional (like C's printf). Hard to read with many variables. |
| **`.format()` Method** | `"{0} is {1}".format("Age", 30)` | Medium | Very flexible. Can separate template from arguments. Still verbose. |
| **f-Strings (Modern)** | `f"{name} is {age}"` | Fastest | Most readable and clean. Evaluated at runtime. |

**Input:**
```python
name = "Charlie"
age = 30

# 1. Old-School % formatting
percent_fmt = "Hello, %s. You are %d years old." % (name, age)

# 2. .format() method
dot_fmt = "Hello, {}. You are {} years old.".format(name, age)

# 3. f-string
f_fmt = f"Hello, {name}. You are {age} years old."

print("Percent Format:", percent_fmt)
print("Dot Format    :", dot_fmt)
print("f-String      :", f_fmt)
```
**Output:**
```text
Percent Format: Hello, Charlie. You are 30 years old.
Dot Format    : Hello, Charlie. You are 30 years old.
f-String      : Hello, Charlie. You are 30 years old.
```

---


## 9. Joining Strings (`.join()`)

While `+` is used to join two string variables, what if you have a massive List of 100 strings that you want to combine into a single string? You use the `.join()` method!

The `.join()` method takes all items in an iterable (like a List or Tuple) and joins them into one string using a specified separator.

**Input:**
```python
# A list of words
words = ["Python", "is", "a", "great", "language"]

# Join them with a space
sentence = " ".join(words)
print("With Spaces:", sentence)

# Join them with a dash
dashed = "-".join(words)
print("With Dashes:", dashed)
```
**Output:**
```text
With Spaces: Python is a great language
With Dashes: Python-is-a-great-language
```

<br>
<h3 align="center">
  <a href="1.%20Modifying%20and%20Splitting%20Strings.md"><kbd style="font-size: 20px; padding: 5px 10px;">&nbsp;⏮️ Prev&nbsp;</kbd></a>
  &nbsp;&nbsp;&nbsp;&nbsp;
  <a href="3.%20Escape%20Characters%20and%20Boolean%20Methods.md"><kbd style="font-size: 20px; padding: 5px 10px;">&nbsp;Next ⏭️&nbsp;</kbd></a>
</h3>
