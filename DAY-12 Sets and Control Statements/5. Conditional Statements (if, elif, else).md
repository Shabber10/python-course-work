### 1. Conditional Statements (`if`, `elif`, `else`)
Used to execute code based on conditions.
**Input:**
```python
x = 10
if x > 15:
    print("Greater than 15")
elif x == 10:
    print("Exactly 10")
else:
    print("Less than 10")
```
**Output:**
```text
Exactly 10
```

