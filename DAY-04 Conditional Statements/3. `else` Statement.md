## 3. `else` Statement
The `else` keyword catches anything which isn't caught by the preceding conditions.

**Input:**
```python
number = 7
if number % 2 == 0:
    print("Even number")
else:
    print("Odd number")
```
**Output:**
```text
Odd number
```

