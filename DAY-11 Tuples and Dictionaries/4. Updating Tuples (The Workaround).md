### 4. Updating Tuples (The Workaround)
Since tuples are immutable, convert to list -> change -> convert back.
**Input:**
```python
x = ("apple", "banana", "cherry")
y = list(x)
y[1] = "kiwi"
x = tuple(y)
print(x)
```
**Output:**
```text
('apple', 'kiwi', 'cherry')
```

